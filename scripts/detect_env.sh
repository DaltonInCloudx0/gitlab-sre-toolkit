#!/bin/bash
# Detect the type of GitLab deployment on a target host.
#
# This script is a placeholder. In a future milestone it will attempt to detect
# whether the target is running an Omnibus package, a containerised GitLab or
# something else and print the detected deployment type.

echo "detect_env.sh: environment detection is not implemented yet."
exit 0